import styled from "styled-components";
import img from "./assets/empty_card.jpg";

export const Root = styled.div`
	background: url(${img}) no-repeat;
	background-size: cover;
	border-radius: 8px;
	border: 1px solid #000;
`;
